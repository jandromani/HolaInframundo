const RAW='https://raw.githubusercontent.com/jandromani/HolaInframundo/master/';
const FALLBACK_WINDOW={
  id:'ASTRO_OPEC_2026_09_30_10_05',
  title:'VENTANA ESPECIAL · ASTROECONOMÍA × OPEC+',
  start:'2026-09-30T00:00:00+02:00',
  core_start:'2026-10-02T00:00:00+02:00',
  core_end:'2026-10-04T23:59:59+02:00',
  end:'2026-10-05T23:59:59+02:00',
  opec:{date:'2026-10-04',label:'Reunión mensual OPEC+',source_url:'https://www.opec.org/pr-detail/613-6-september-2026.html'},
  groups:[]
};
let WINDOW=FALLBACK_WINDOW,CUR={mechanisms:{}},INV={strategies:{}},PRICING={observations:{}},MARKET={metrics:{}},PROD={},BRIEF={};
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const clamp=(x,a=0,b=100)=>Math.max(a,Math.min(b,Number(x)||0));
const fmt=n=>Number.isFinite(Number(n))?Math.round(Number(n)):'—';
async function get(path,fallback){try{const r=await fetch(RAW+path+'?t='+Date.now(),{cache:'no-store'});if(!r.ok)throw new Error(String(r.status));return await r.json()}catch{return fallback}}
function ageText(iso){const t=Date.parse(iso||'');if(!Number.isFinite(t))return 'sin fecha';const m=Math.max(0,(Date.now()-t)/60000);if(m<90)return `${Math.round(m)} min`;const h=m/60;if(h<48)return `${h.toFixed(h<10?1:0)} h`;return `${Math.round(h/24)} d`}
function phase(){const now=Date.now(),start=Date.parse(WINDOW.start),core=Date.parse(WINDOW.core_start),coreEnd=Date.parse(WINDOW.core_end),end=Date.parse(WINDOW.end);if(now<start)return {key:'PREP',label:'PREPARACIÓN',target:start,targetLabel:'apertura'};if(now<core)return {key:'OPEN',label:'VENTANA ABIERTA',target:core,targetLabel:'núcleo'};if(now<=coreEnd)return {key:'CORE',label:'NÚCLEO ROJO',target:coreEnd,targetLabel:'fin núcleo'};if(now<=end)return {key:'DISCOVERY',label:'PRICE DISCOVERY',target:end,targetLabel:'cierre'};return {key:'POST',label:'POST-MORTEM',target:null,targetLabel:''}}
function countdown(target){if(!target)return 'VENTANA CERRADA';let ms=Math.max(0,target-Date.now());const d=Math.floor(ms/86400000);ms-=d*86400000;const h=Math.floor(ms/3600000);return d?`${d}d ${h}h`:`${h}h`}
function mech(id){return CUR.mechanisms?.[id]||null}
function strat(id){return INV.strategies?.[id]||null}
function priced(id,m){const s=strat(id);return clamp(s?.priced_in??m?.crowd?.priced_in)}
function candidateSymbols(id){const s=strat(id);return (s?.top5||s?.top_candidates||[]).map(x=>x.symbol).filter(Boolean)}
function groupStats(g){const rows=(g.mechanisms||[]).map(id=>{const m=mech(id);if(!m)return null;const p=priced(id,m),c=clamp(m.score),gap=c-p,delta=Number(m.score_delta||0);const symbols=candidateSymbols(id);const returns=symbols.map(s=>Number(MARKET.metrics?.[s]?.ret20)).filter(Number.isFinite);const ret20=returns.length?returns.reduce((a,b)=>a+b,0)/returns.length:null;const priority=clamp(.58*c+.26*Math.max(0,gap)+.16*clamp(50+delta*3));return {id,label:m.label||id,state:m.state||'UNKNOWN',causal:c,priced:p,gap,delta,priority,ret20,symbols}}).filter(Boolean).sort((a,b)=>b.priority-a.priority);const focus=rows.slice(0,Math.min(2,rows.length));const avg=k=>focus.length?focus.reduce((a,x)=>a+(Number(x[k])||0),0)/focus.length:0;const rets=focus.map(x=>x.ret20).filter(Number.isFinite);return {rows,causal:avg('causal'),priced:avg('priced'),gap:avg('gap'),priority:avg('priority'),ret20:rets.length?rets.reduce((a,b)=>a+b,0)/rets.length:null}}
function blindSensors(){const direct=Object.values(PRICING.observations||{}).filter(x=>x?.status&&x.status!=='OK').map(x=>({name:x.source||x.metric||'sensor',status:x.status}));const blocks=Object.entries(PROD.pricing?.blocks||{}).flatMap(([name,status])=>{const vals=Array.isArray(status)?status:[status];return vals.some(x=>x!=='OK')?[{name,status:vals.join('/')}]:[]});const seen=new Set();return [...direct,...blocks].filter(x=>{const k=x.name+'|'+x.status;if(seen.has(k))return false;seen.add(k);return true})}
function riskLabel(v){if(v>=70)return ['TENSIÓN ALTA','hot'];if(v>=52)return ['TENSIÓN CRECIENTE','warm'];if(v>=35)return ['VIGILANCIA','watch'];return ['TEMPRANO','cool']}
function styles(){if(document.querySelector('#gearwatch-event-window-style'))return;const st=document.createElement('style');st.id='gearwatch-event-window-style';st.textContent=`
.event-window{position:relative;overflow:hidden;margin:-2px 0 14px;border:1px solid #6f3c31;border-radius:18px;background:radial-gradient(circle at 6% 0%,#542117 0,transparent 31%),radial-gradient(circle at 95% 10%,#452d12 0,transparent 27%),linear-gradient(135deg,#130b0b,#0b1218 55%,#10151b);box-shadow:0 20px 60px #0008,0 0 0 1px #341b17 inset}.event-window:before{content:'';position:absolute;inset:0;background:linear-gradient(110deg,transparent 0 42%,#ffb45a0d 48%,transparent 54%);pointer-events:none}.ew-top{display:grid;grid-template-columns:minmax(0,1.3fr) auto;gap:18px;padding:17px 18px 12px;align-items:center}.ew-kicker{font-size:10px;font-weight:950;letter-spacing:.2em;text-transform:uppercase;color:#ff9d79}.ew-title{font-size:clamp(22px,3.3vw,44px);line-height:.98;letter-spacing:-.035em;font-weight:950;margin:6px 0 8px}.ew-sub{max-width:920px;color:#c9d5dc;font-size:12px}.ew-clock{text-align:right;min-width:190px}.ew-phase{display:inline-flex;align-items:center;gap:7px;border:1px solid #754536;border-radius:999px;padding:5px 9px;font-size:9px;font-weight:950;letter-spacing:.08em}.ew-phase:before{content:'';width:7px;height:7px;border-radius:50%;background:#ff805e;box-shadow:0 0 14px #ff805e}.ew-count{font:950 clamp(28px,4vw,46px)/1 ui-monospace,SFMono-Regular,Menlo,monospace;margin-top:8px}.ew-countlabel{font-size:9px;color:#98aeb9;text-transform:uppercase;letter-spacing:.12em;margin-top:3px}.ew-live{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;padding:0 18px 12px}.ew-group{border:1px solid #2c3d46;background:#071218d6;border-radius:12px;padding:10px;min-height:96px}.ew-group.hot{border-color:#874a36;background:#1b1010d9}.ew-group.warm{border-color:#67502c}.ew-group .label{font-size:9px;letter-spacing:.11em;text-transform:uppercase;font-weight:900;color:#9fb4bf}.ew-num{font-size:23px;font-weight:950;margin:3px 0}.ew-bars{display:grid;grid-template-columns:1fr 1fr;gap:5px;font-size:9px;color:#8fa3ad}.ew-meter{height:5px;background:#14232b;border-radius:999px;overflow:hidden;margin-top:3px}.ew-meter i{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,#ff8a66,#ffd166)}.ew-row{display:flex;justify-content:space-between;gap:8px;margin-top:6px;color:#b9cad2;font-size:9px}.ew-strip{display:grid;grid-template-columns:1.2fr 1fr 1fr 1fr;gap:1px;border-top:1px solid #382621;background:#382621}.ew-cell{background:#0a1217;padding:8px 12px;font-size:10px}.ew-cell b{display:block;color:#f5fbff;margin-bottom:2px}.ew-cell a{color:#ffd166;text-decoration:none}.ew-details{border-top:1px solid #382621}.ew-details summary{cursor:pointer;padding:9px 18px;color:#a8bbc4;font-size:10px;font-weight:850}.ew-detailgrid{display:grid;grid-template-columns:1.05fr .95fr;gap:10px;padding:0 18px 16px}.ew-timeline,.ew-mechs{border:1px solid #263a44;border-radius:12px;padding:11px;background:#071219}.ew-tl{display:grid;grid-template-columns:72px 1fr;gap:8px;padding:6px 0;border-top:1px solid #152832;font-size:10px}.ew-tl:first-of-type{border-top:0}.ew-tl strong{color:#ffbd74}.ew-mrow{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:7px 0;border-top:1px solid #152832;font-size:10px}.ew-mrow:first-of-type{border-top:0}.ew-state{font:900 9px ui-monospace,monospace;color:#ffd166}.ew-blind{color:#ff9f90}.ew-legend{font-size:9px;color:#8499a4;margin-top:8px}@media(max-width:980px){.ew-live{grid-template-columns:1fr 1fr}.ew-strip{grid-template-columns:1fr 1fr}.ew-detailgrid{grid-template-columns:1fr}}@media(max-width:620px){.event-window{border-radius:14px}.ew-top{grid-template-columns:1fr;padding:14px 13px 10px}.ew-clock{text-align:left;min-width:0}.ew-live{grid-template-columns:1fr;padding:0 13px 10px}.ew-strip{grid-template-columns:1fr}.ew-detailgrid{padding:0 13px 13px}.ew-details summary{padding-left:13px}.ew-title{font-size:29px}}
`;document.head.appendChild(st)}
function render(){styles();let host=document.querySelector('#event-window');if(!host){host=document.createElement('section');host.id='event-window';host.className='event-window';document.querySelector('.wrap')?.prepend(host)}else host.className='event-window';const p=phase();const stats=(WINDOW.groups||[]).map(g=>({g,s:groupStats(g)}));const overall=stats.length?stats.reduce((a,x)=>a+x.s.priority,0)/stats.length:0;const [overallLabel]=riskLabel(overall);const blind=blindSensors();const errors=Object.entries(CUR.health||{}).filter(([k,v])=>k.endsWith('_errors')&&Number(v)>0).reduce((a,[,v])=>a+Number(v),0);const astro=(WINDOW.astro_markers||[]).map(x=>`<div class="ew-tl"><strong>${esc(x.date_label||x.date)}</strong><span>${esc(x.label)}${x.role?` · <span style="color:#8499a4">${esc(x.role)}</span>`:''}</span></div>`).join('');const focusRows=stats.flatMap(x=>x.s.rows.slice(0,2).map(r=>({...r,group:x.g.label}))).sort((a,b)=>b.priority-a.priority).slice(0,7);host.innerHTML=`<div class="ew-top"><div><div class="ew-kicker">🔴 ${esc(WINDOW.title||FALLBACK_WINDOW.title)}</div><div class="ew-title">30 SEP → 5 OCT · SHOCK WATCH</div><div class="ew-sub">La astrología abre una <b>ventana temporal de hipótesis</b>; GearWatch sólo la eleva cuando la confirman mecanismos físicos, mercado y sensores. Hoy: <b>${esc(overallLabel)}</b> · presión causal compuesta ${fmt(overall)}/100.</div></div><div class="ew-clock"><div class="ew-phase">${esc(p.label)}</div><div class="ew-count">${esc(countdown(p.target))}</div><div class="ew-countlabel">hasta ${esc(p.targetLabel||'post-mortem')}</div></div></div><div class="ew-live">${stats.map(({g,s})=>{const [lab,cls]=riskLabel(s.priority);const chase=Number.isFinite(s.ret20)&&s.ret20>12;return `<div class="ew-group ${cls}"><div class="label">${esc(g.icon||'⚙')} ${esc(g.label)}</div><div class="ew-num">${fmt(s.priority)}<span style="font-size:10px;color:#8ea5b3"> /100</span></div><div class="ew-bars"><div>causal ${fmt(s.causal)}<div class="ew-meter"><i style="width:${clamp(s.causal)}%"></i></div></div><div>descontado ${fmt(s.priced)}<div class="ew-meter"><i style="width:${clamp(s.priced)}%;filter:saturate(.55)"></i></div></div></div><div class="ew-row"><span>${esc(lab)}</span><span>${s.gap>=0?'+':''}${fmt(s.gap)} gap${chase?' · ⚠ chase':''}</span></div></div>`}).join('')}</div><div class="ew-strip"><div class="ew-cell"><b>🛢 OPEC+ · 4 OCT</b><span>Reunión mensual confirmada · <a href="${esc(WINDOW.opec?.source_url||'#')}" target="_blank" rel="noreferrer">fuente OPEC ↗</a></span></div><div class="ew-cell"><b>⚙ Último scan</b><span>${esc(ageText(CUR.updated_at))} · ${fmt(CUR.health?.mechanisms)} mecanismos</span></div><div class="ew-cell"><b>◉ Sensores</b><span>${fmt(PROD.pricing?.live_sources)} live · ${fmt(PROD.pricing?.scored_sources)} scored · ${blind.length} con atención</span></div><div class="ew-cell"><b>⚖ Producción</b><span>${esc(PROD.status||'UNKNOWN')} · judge ${esc(PROD.judge?.status||'—')} · errores ${errors}</span></div></div><details class="ew-details"><summary>ABRIR MAPA DE LA VENTANA · aspectos, mecanismos líderes y sensores ciegos</summary><div class="ew-detailgrid"><div class="ew-timeline"><div class="ew-kicker">CALENDARIO PRE-REGISTRADO</div>${astro}<div class="ew-legend">Los aspectos astrológicos no se tratan como evidencia causal. Sirven para pre-registrar la ventana antes del evento y poder medir después si añadió señal o sólo coincidencia.</div></div><div class="ew-mechs"><div class="ew-kicker">MECANISMOS QUE DECIDEN SI LA VENTANA “ENCIENDE”</div>${focusRows.map(r=>`<div class="ew-mrow"><div><b>${esc(r.label)}</b><div style="color:#8298a3">${esc(r.group)} · causal ${fmt(r.causal)} · priced ${fmt(r.priced)} · Δ ${r.delta>=0?'+':''}${fmt(r.delta)}${Number.isFinite(r.ret20)?` · piezas 20d ${r.ret20>=0?'+':''}${r.ret20.toFixed(1)}%`:''}</div></div><span class="ew-state">${esc(r.state)}</span></div>`).join('')||'<div class="ew-legend">Sin mecanismos disponibles.</div>'}${blind.length?`<div class="ew-legend ew-blind"><b>Sensores con atención:</b> ${blind.slice(0,6).map(x=>`${esc(x.name)}=${esc(x.status)}`).join(' · ')}</div>`:''}</div></div></details>`}
async function init(){[WINDOW,CUR,INV,PRICING,MARKET,PROD,BRIEF]=await Promise.all([get('data/event-window.json',FALLBACK_WINDOW),get('data/current.json',{mechanisms:{}}),get('data/investment-layer.json',{strategies:{}}),get('data/pricing-sensors.json',{observations:{}}),get('data/market.json',{metrics:{}}),get('data/production-health.json',{}),get('data/daily-briefing.json',{})]);render();setInterval(render,60000)}
init();
